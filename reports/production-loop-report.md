# 最小生产闭环自测报告

- 场景通过：12/12
- 检查点通过：34/34
- 范围：告警治理、可信身份、责任链和安全边界基线
- 重要限制：通过不等于已在十万台真实设备环境验证

| 编号 | 场景 | 结果 | 验证重点 |
|---|---|---|---|
| PL-01 | TOR上游故障抑制下游服务器断连 | 通过 | 下游告警保留但被抑制，并关联上游事故 |
| PL-02 | 端口抖动后收到恢复信号 | 通过 | 原告警进入恢复待验证，事故不自动关闭 |
| PL-03 | 维护窗口内产生相同告警 | 通过 | 告警被维护静默且不创建事故 |
| PL-04 | 采集器断开导致设备无数据 | 通过 | 来源标记为链路异常，不生成设备事故 |
| PL-05 | OMS机架位与现场扫码记录冲突 | 通过 | 生成身份冲突并阻止现场操作 |
| PL-06 | 故障前存在网卡配置变更 | 通过 | 变更只作为候选证据 |
| PL-07 | BMC与OMS报告的完整SN不一致 | 通过 | 保留两个来源并生成阻断冲突 |
| PL-08 | 区域温升与单点传感器异常并存 | 通过 | 不同实体不自动合并，等待动环拓扑或人工确认 |
| PL-09 | 夜间单人值班接到P1事故 | 通过 | 记录值班、分派、确认与升级负责人 |
| PL-10 | 重复告警恢复后再次复发 | 通过 | 持续期内去重，恢复后的复发生成新活动告警 |
| PL-11 | 监控恢复但业务仍未验证 | 通过 | 明确要求业务验证，事故保持原状态 |
| PL-12 | 两个无关故障同时出现 | 通过 | 两个实体和信号形成两个独立事故 |

## 逐项结果

### PL-01 TOR上游故障抑制下游服务器断连

- 通过｜下游生命周期：实际 `suppressed`，预期 `suppressed`
- 通过｜下游不新建事故：实际 `False`，预期 `False`
- 通过｜复用上游事故：实际 `INC-PL-001`，预期 `INC-PL-001`

### PL-02 端口抖动后收到恢复信号

- 通过｜更新同一告警：实际 `ALT-02B2B8CDF927`，预期 `ALT-02B2B8CDF927`
- 通过｜恢复待验证：实际 `True`，预期 `True`
- 通过｜事故未自动关闭：实际 `new`，预期 `new`

### PL-03 维护窗口内产生相同告警

- 通过｜维护静默：实际 `silenced`，预期 `silenced`
- 通过｜不创建事故：实际 `False`，预期 `False`
- 通过｜仍保存告警：实际 `1`，预期 `1`

### PL-04 采集器断开导致设备无数据

- 通过｜识别采集链路问题：实际 `True`，预期 `True`
- 通过｜覆盖率：实际 `0.0`，预期 `0.0`
- 通过｜不伪造设备事故：实际 `0`，预期 `0`

### PL-05 OMS机架位与现场扫码记录冲突

- 通过｜生成冲突：实际 `1`，预期 `1`
- 通过｜保护字段阻止操作：实际 `True`，预期 `True`
- 通过｜不静默覆盖OMS值：实际 `BJYZ-A-01-01`，预期 `BJYZ-A-01-01`

### PL-06 故障前存在网卡配置变更

- 通过｜变更已保存：实际 `1`，预期 `1`
- 通过｜因果等级：实际 `candidate_only`，预期 `candidate_only`

### PL-07 BMC与OMS报告的完整SN不一致

- 通过｜生成冲突：实际 `1`，预期 `1`
- 通过｜保护字段阻止操作：实际 `True`，预期 `True`
- 通过｜不静默覆盖OMS值：实际 `SERVER-SN-001`，预期 `SERVER-SN-001`

### PL-08 区域温升与单点传感器异常并存

- 通过｜不同实体不自动合并：实际 `True`，预期 `True`
- 通过｜保留两个事故：实际 `2`，预期 `2`

### PL-09 夜间单人值班接到P1事故

- 通过｜当前值班可查：实际 `1`，预期 `1`
- 通过｜值班确认收到：实际 `acknowledged`，预期 `acknowledged`
- 通过｜升级负责人：实际 `lead-on-call`，预期 `lead-on-call`

### PL-10 重复告警恢复后再次复发

- 通过｜持续期内去重：实际 `ALT-F9F89DB227D8`，预期 `ALT-F9F89DB227D8`
- 通过｜重复次数：实际 `2`，预期 `2`
- 通过｜复发生成新告警：实际 `True`，预期 `True`
- 通过｜复发生成新事故：实际 `2`，预期 `2`

### PL-11 监控恢复但业务仍未验证

- 通过｜要求业务验证：实际 `True`，预期 `True`
- 通过｜保留事故关联：实际 `INC-PL-001`，预期 `INC-PL-001`
- 通过｜事故仍未解决：实际 `new`，预期 `new`

### PL-12 两个无关故障同时出现

- 通过｜事故ID不同：实际 `True`，预期 `True`
- 通过｜两个独立事故：实际 `2`，预期 `2`
